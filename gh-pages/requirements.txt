# Python dependencies for building the Windows MCP Server documentation site.
# Build locally with:  pip install -r requirements.txt && mkdocs build
mkdocs-material==9.7.7
mkdocs-minify-plugin==0.8.0
