[project]
name = "windows-usage-evals"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "pytest>=9.0",
    "pytest-asyncio>=1.3",
    "pytest-skill-engineering",
    "psutil>=7.0",
]

[tool.uv.sources]
pytest-skill-engineering = { git = "https://github.com/sbroenne/pytest-skill-engineering", rev = "5a533dcd149c3e27f60b0a296a047ba1d3f255ff" }

[dependency-groups]
dev = ["ruff>=0.15"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
asyncio_mode = "auto"
addopts = "--strict-markers"
markers = ["usage_live: manually approved Windows desktop and model usage"]

[tool.ruff]
line-length = 100
target-version = "py312"

[tool.ruff.lint]
select = ["E", "F", "B", "I"]
